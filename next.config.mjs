/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "rainbowit.net",
          "images.pexels.com",
          "img.freepik.com"
        ],
      },
};

export default nextConfig;
